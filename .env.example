# Example environment variables
PORT=3000
JWT_SECRET=replace_with_secure_secret

# Supabase (optional). If set, server will use Supabase/Postgres instead of local SQLite
SUPABASE_URL=
SUPABASE_ANON_KEY=
# For server-side operations you may use the service role key
SUPABASE_SERVICE_KEY=
# Copy to .env and change values for production
PORT=3000
JWT_SECRET=replace_this_with_a_secure_secret
NODE_ENV=production
