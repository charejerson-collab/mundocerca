# ===========================================
# MundoCerca Environment Configuration
# ===========================================
# Copy this file to .env and update the values

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (REQUIRED in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_secure_random_secret_here

# Supabase (optional - if set, uses Supabase/Postgres instead of local SQLite)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=

# ===========================================
# Email Service (for Password Reset)
# ===========================================
# Option 1: SendGrid (Recommended for production)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your_sendgrid_api_key_here
SMTP_FROM=noreply@mundocerca.com

# Option 2: Mailtrap (For development/testing)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=your_mailtrap_username
# SMTP_PASS=your_mailtrap_password

# ===========================================
# Password Reset Configuration
# ===========================================
OTP_TTL_MINUTES=10
RESEND_COOLDOWN_SECONDS=60
MAX_VERIFY_ATTEMPTS=5
MAX_RESET_REQUESTS_PER_EMAIL_HOUR=3
MAX_RESET_REQUESTS_PER_IP_HOUR=10
